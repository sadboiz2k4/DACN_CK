spring:
  datasource:
    url: ${SPRING_DATASOURCE_URL:jdbc:mysql://localhost:3306/smartspend?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC&characterEncoding=UTF-8&useUnicode=true}
    username: root
    password:
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: false
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect
        format_sql: true
  mail:
    host: ${MAIL_HOST:smtp.gmail.com}
    port: ${MAIL_PORT:587}
    username: ${MAIL_USERNAME:testsystem2004@gmail.com}
    password: ${MAIL_PASSWORD:nynubzkhivjlusld}
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true
          ssl:
            trust: ${MAIL_HOST:smtp.gmail.com}
          connectiontimeout: 10000
          timeout: 10000
          writetimeout: 10000

server:
  port: 8080

app:
  frontend-url: ${APP_FRONTEND_URL:http://localhost:3000}

jwt:
  secret: ${JWT_SECRET:smartspend-jwt-secret-key-2024-very-long-and-secure}
  expiration: 86400000

ai:
  service:
    url: ${AI_SERVICE_URL:http://localhost:8000}
  groq:
    key: ${AI_KEY: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}

google:
  client-id: ${GOOGLE_CLIENT_ID:1070878898814-svtnq77h0q7drqf9lt1803se85kj1487.apps.googleusercontent.com}

logging:
  level:
    com.smartspend: DEBUG
